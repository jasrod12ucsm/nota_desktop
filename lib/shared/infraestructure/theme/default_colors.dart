import 'package:flutter/material.dart';

class DefaultColors {
  final Color correctColor = const Color.fromRGBO(76, 175, 80, 1.0);
   final Color errorColor =
      const Color.fromRGBO(102, 51, 153, 1.0); // Royal Purple
  final primaryColor = const Color.fromRGBO(248, 90, 98, 1.0);
  final secondaryColor = const Color.fromRGBO(248, 178, 178, 1.0);
  final surfaceColor = Colors.grey[100];
  final Color tertiaryColor =
      const Color.fromRGBO(100, 150, 200, 1.0); // Color terciario
      
  DefaultColors();

  
}